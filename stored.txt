if self.check_winner() {
            if self.current_turn == self.robot_turn {
                self.history.push('w');
                list.push(self.history.clone());
                //return;
        } else {
                self.history.push('l');
                list.push(self.history.clone());
                //return;
            }
        
        } 